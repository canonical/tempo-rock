name: tempo
summary: Tempo in a ROCK.
description: "Tempo is a distributed tracing backend by Grafana."
version: "2.4.0"
base: ubuntu:22.04
license: AGPL-3.0
services:
  tempo:
    command: /bin/tempo --config.file=/etc/tempo/tempo.yaml
    override: replace
    startup: enabled
platforms:
  amd64:
parts:
  tempo:
    plugin: go
    source: https://github.com/grafana/tempo
    source-type: git
    source-tag: "v2.4.0"
    build-snaps:
      - go/1.20/stable
    build-environment:
      - BUILD_IN_CONTAINER: "false"
    build-packages:
      - libsystemd-dev
    override-build: |
      install -D -m755 ./cmd/tempo/tempo ${CRAFT_PART_INSTALL}/usr/bin/tempo
    stage:
      - usr/bin/tempo
  default-config:
    plugin: dump
    source: .
    organize:
      tempo.yaml: etc/tempo/tempo.yaml
    stage:
      - etc/tempo/tempo.yaml
  ca-certs:
    plugin: nil
    overlay-packages: [ca-certificates]
  deb-security-manifest:
    plugin: nil
    after:
      - tempo
      - ca-certs
    override-prime: |
      set -x
      mkdir -p $CRAFT_PRIME/usr/share/rocks/
      (echo "# os-release" && cat /etc/os-release && echo "# dpkg-query" && dpkg-query --admindir=$CRAFT_PRIME/var/lib/dpkg/ -f '${db:Status-Abbrev},${binary:Package},${Version},${source:Package},${Source:Version}\n' -W) > $CRAFT_PRIME/usr/share/rocks/dpkg.query
